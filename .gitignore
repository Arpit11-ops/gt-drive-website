node_modules/
.next/
out/
*.log
.DS_Store
.worktrees/

# TS incremental build cache
tsconfig.tsbuildinfo

# Owner-supplied raw photo dump — kept locally, never shipped
SCOOTER PHOTOS (1)/

# Pre-existing user-owned shortlist doc — kept local per handoff
GT_DRIVE_COMPONENT_SHORTLIST.html
